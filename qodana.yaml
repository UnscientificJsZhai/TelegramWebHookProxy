version: "1.0"
linter: jetbrains/qodana-jvm-community:2026.1

projectJDK: "21"

rootJavaProjects:
  - "."

profile:
  name: qodana.recommended

bootstrap: ./gradlew --no-daemon :backend:classes :backend:testClasses

failureConditions:
  severityThresholds:
    critical: 0
    high: 0
